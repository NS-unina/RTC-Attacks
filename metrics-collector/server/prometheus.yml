global:
  scrape_interval: 15s 

scrape_configs:
  - job_name: 'remote-target'
    static_configs:
      - targets: ['10.0.0.245:9100'] 
  - job_name: 'pushgateway'
    honor_labels: true 
    static_configs:
      - targets: ['pushgateway:9091'] # o l'IP del server se Prometheus è remoto